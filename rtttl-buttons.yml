packages:
  rtttl-scripts: !include rtttl-scripts.yml

button:
  - platform: template
    name: "Test Beep"
    icon: "mdi:volume-high"
    entity_category: diagnostic
    disabled_by_default: true
    on_press:
      - script.execute: play_beep
  - platform: template
    name: "Test Mario Beep"
    icon: "mdi:volume-high"
    entity_category: diagnostic
    disabled_by_default: true
    on_press:
      - script.execute: play_mario
  - platform: template
    name: "Test Two Short Beeps"
    icon: "mdi:volume-high"
    entity_category: diagnostic
    disabled_by_default: true
    on_press:
      - script.execute: play_two_short
  - platform: template
    name: "Test Long Beep"
    icon: "mdi:volume-high"
    entity_category: diagnostic
    disabled_by_default: true
    on_press:
      - script.execute: play_long
  - platform: template
    name: "Test Siren Beep"
    icon: "mdi:volume-high"
    entity_category: diagnostic
    disabled_by_default: true
    on_press:
      - script.execute: play_siren
  - platform: template
    name: "Test Scale Up Beep"
    icon: "mdi:volume-high"
    entity_category: diagnostic
    disabled_by_default: true
    on_press:
      - script.execute: play_scale_up
  - platform: template
    name: "Test Star Wars Beep"
    icon: "mdi:volume-high"
    entity_category: diagnostic
    disabled_by_default: true
    on_press:
      - script.execute: play_star_wars
  - platform: template
    name: "Test Mission Impossible Beep"
    icon: "mdi:volume-high"
    entity_category: diagnostic
    disabled_by_default: true
    on_press:
      - script.execute: play_mission_impossible
